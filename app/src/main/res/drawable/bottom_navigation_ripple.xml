<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Ripple effect when selected -->
    <item android:state_selected="true" android:color="@color/colorNeutralBackground" />
    <!-- Ripple effect when pressed -->
    <item android:state_pressed="true" android:color="@color/colorDivider" />
    <!-- Default background -->
    <item android:color="@android:color/transparent" />
</selector>